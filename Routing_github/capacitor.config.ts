import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'assignment8_routing',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
